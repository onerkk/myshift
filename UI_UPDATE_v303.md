# 我的班表 v303・立體介面更新

本包以已完成天氣修正的 v302 為基礎，只包含本次變更的檔案。請在原專案目錄依相同路徑覆蓋，保留其餘檔案；若仍使用 v301 或更早版本，請先套用 v302 天氣更新包。

## 安裝

1. 將 `app.js`、`styles.css`、`index.html`、`sw.js` 覆蓋到原專案根目錄；其餘檔案依原路徑加入。
2. 按原有方式更新網站，連網重新開啟 App。Service Worker 與前端資源版本已同步升為 v303。

本包不需要重新設定 Firebase、重建班表或清除個人資料。`weather-data.js` 沿用 v302，因此未重複打包。

## 這次更新

- **整體**：深藍、霧白與琥珀色；重新統一字級、間距、邊框與明暗層次，移除歷代重疊樣式。採用 CSS 製作卡片厚度、邊緣反光、立體圖示底座、內凹進度條與按壓回饋。
- **今天**：以班次與倒數為主，縮減大日期裝飾；七日班表可直接開啟指定日期，天氣和薪資整合為摘要區。休假日不顯示虛構的工作進度。
- **班表**：單一月份控制列，左鍵上月、右鍵下月；日期改用真正的按鈕，保留調班、請假、行程、發薪與農曆標記，加入簡潔的今日入口與月份統計。
- **薪資**：突出實領金額，整理應領、應扣、工時、發薪日與可展開明細。只有公司核對差額為零時，才顯示已核對文字；計算規則沿用原版。
- **天氣**：修正深色天氣卡的文字對比，保留來源、資料時間、手選位置、重試和更新狀態，重新整理降雨圖、逐日預報與潮汐區。首頁與天氣頁沿用 v302 的自動更新機制。
- **更多**：帳號、偏好設定、請假統計與工具重新分組；移除重複的管理員入口，設定開關改為可鍵盤操作的按鈕，保留原有角色權限。
- **操作**：保留五個固定導覽入口，放大常用控制與表單；支援頁面縮放、夜間配色與系統「減少動態效果」偏好。安裝提示放入內容區，避免覆蓋底部導覽。

## 免費資源

本次介面只使用原生 HTML、CSS、SVG、系統字型與原專案圖示，沒有新增付費素材、付費字型、WebGL 套件或第三方 UI 相依項目。

天氣資料與供應商限制沿用 v302：Open-Meteo 主來源、MET Norway 備援。開啟 App 時每 5 分鐘更新，回到前景或恢復連線時依原有間隔檢查；畫面顯示資料時間，實際資料新鮮度仍取決於供應商。背景通知依瀏覽器與手機支援情況運作。

## 檔案

| 路徑 | 用途 |
| --- | --- |
| `app.js` | 五個頁面的版面、呈現與介面操作 |
| `styles.css` | 統一的立體設計、手機與桌面配置、夜間樣式 |
| `index.html` | v303 資源引用、縮放設定與啟動畫面圖示 |
| `sw.js` | v303 靜態資源快取版本 |
| `tests/ui.test.cjs` | 新版呈現與日期操作的自動測試 |
| `UI_UPDATE_v303.md` | 本更新說明 |

## 驗證範圍

執行 `TZ=Asia/Taipei node --test tests/*.test.cjs`，60 項測試全部通過：原有的 53 項天氣、請假、薪資測試，加上 7 項新版介面測試。涵蓋五頁中／印尼文呈現、跨年切月、指定日期入口、剩餘班次時間、薪資核對標示、天氣操作入口與管理員顯示權限。

根目錄 JavaScript 語法、首頁本地資源路徑、CSS 括號與變數引用均已檢查。未進行瀏覽器畫面檢視或 Android／iOS 實機驗收；自動測試不等於已確認所有裝置的實際視覺效果。本次未部署網站。
