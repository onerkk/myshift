# 天氣修正 v302（2026-09-10）

本 ZIP 只含本次修改／新增的檔案。解壓後，依原本路徑一起覆蓋到專案根目錄；新增的 `weather-data.js` 也必須上傳。部署完成後重新開啟網頁／桌面 App，Service Worker 與核心檔版本已一起更新。

## 這次修正

- 重新載入保留最後有效定位及可用天氣，不再先刪除資料，避免定位暫時失敗就整張卡片失效。
- 定位被拒絕或無法取得時顯示原因，可使用「選擇地點」搜尋城市／鄉鎮；選好後記住選擇。手選地點不會偽裝成目前 GPS 位置發送災防通知。
- 主天氣先顯示；地名反查、官方降雨預報及潮汐各自載入。逾時會中止請求，包括卡住的 JSON 下載。
- 開啟 App 時約每 5 分鐘檢查天氣；回到畫面且距上次查詢超過 1 分鐘時補抓，恢復網路時重新檢查。隱藏時停止前景輪詢，來源限流時尊重冷卻時間。重複點擊共用同一筆查詢。
- Open-Meteo 失敗時自動使用 MET Norway 備援。明示來源與資料時間；備援沒有提供的降雨機率／陣風保持空白，不當成 0；MET 風速由 m/s 正確換算為 km/h。
- 離線保留最多 6 小時的已驗證天氣，清楚標示離線或舊資料。舊資料不再觸發模式天氣警報／動畫。來源實際資料時間與本機取得時間分開保存。
- Service Worker 不再攔截外部天氣 API；不會刪掉經緯度查詢參數後混用不同地點快取，也不會把 HTML 當成 JavaScript／天氣回應。
- 官方預報回傳的地點必須核對；不符時使用明示來源的模式預報。潮汐依測站距離選取、按時間排序，剔除過期／遠期資料及缺漏潮高。

## 免費資源與更新限制

這次不需要新增 API 金鑰、付費方案、付費代理或伺服器服務。依目前個人、無廣告／訂閱的班表用途使用免費來源：

- Open-Meteo：免費 API 適用非商業使用，有使用量限制。本次保留其模式預報並降低重複請求。[官方文件](https://open-meteo.com/en/docs)、[免費使用條款](https://open-meteo.com/en/terms)。
- MET Norway：免費公開天氣服務，備援遵守快取／限流要求，使用瀏覽器的 Origin 識別來源；已加入來源與授權標示。[服務條款](https://docs.api.met.no/doc/TermsOfService)、[授權](https://docs.api.met.no/doc/License)。
- 地點搜尋：Open-Meteo／GeoNames；反查沿用 OpenStreetMap／BigDataCloud。已加入地點搜尋來源標示。
- 官方警特報、雨量、潮汐沿用專案既有 CWA 代理。本 ZIP 不含代理的雲端原始碼，因此未修改或重新部署雲端 Worker。

「每 5 分鐘檢查」是 App 抓取頻率，並不代表來源每 5 分鐘產生新預報。Open-Meteo current 是模式資料；實際更新依氣象模式／觀測站週期。App 關閉後的背景執行由手機系統決定，不能保證秒級或固定間隔。若改為商業、付費或廣告用途，Open-Meteo 免費方案不適用，不能宣稱可無限制免費商用。

## 已完成驗證

- JavaScript 語法檢查：`app.js`、`weather-data.js`、`sw.js`。
- `TZ=Asia/Taipei node --test tests/*.test.cjs`：53 項通過（24 項天氣／快取測試，加上原有 29 項請假／薪資測試）。
- 實際 HTTP 查詢取得 Open-Meteo、MET Norway、地點搜尋、CWA 統合／預報／潮汐 JSON；Open-Meteo 與 MET Norway 回應通過新解析器，均解析出 7 日資料。這是一次連線驗證，不能保證第三方服務永遠可用。
- 查詢「臺南市鹽水區」時，既有 CWA 預報代理曾回傳 `town: 嘉義縣`；新版本拒絕將這筆資料套成鹽水預報。
- 測試涵蓋來源失敗與備援、JSON 逾時中止、429 冷卻、並發合併、定位失敗、手選位置、斷網、舊／錯地點快取、回到畫面與定期更新、較晚回應不覆蓋新位置、潮汐排序與缺值。

限制：執行環境未安裝瀏覽器，瀏覽器下載未獲網路核准，因此沒有完成手機尺寸的瀏覽器畫面測試；也未登入或操作你的正式帳號、未在你的手機實機驗證。
