# v304 薪資修正更新包

以先前 v303 日夜間立體介面版本為基礎，僅包含本次變更檔。保留原有免費天氣服務與介面。

## 更新網站

1. 把 `site-files/` **裡面的檔案**覆蓋到網站原目錄，保留原有路徑；不要把 `site-files` 當成新的網站子目錄。
2. 必須一起更新 `app.js`、`payroll.js`、`styles.css`、`index.html`、`sw.js`。其中 `payroll.js` 是新增的必要檔案。
3. 等原本的網站發布完成後，重新開啟 App，在「更多」底部確認 **v304**。此次未代為發布網站。
4. `verification/tests/` 是本次更新的測試原始碼，僅供本機驗證。要重跑時放進本機 v303 專案的 `tests/` 目錄，再套用上述更新；既有未變更的天氣測試保持原位。

## 載入你的 8 月薪資條

1. 先將 `private-import/2026-08-payroll.json` 存到手機。
2. App → 薪資 → 右上設定 →「選擇薪資匯入檔」。
3. 選取上述 JSON，預覽 **2026-08／應領 60,467／應扣 6,829／實領 53,638**，按「儲存這張薪資條」。
4. App 會切到 8 月，以「公司實領 · 薪資條記錄」呈現 53,638 元，並在下方另列班表估算與差額。

匯入同時把 8 月的提案獎金設為 800 元、病假扣薪時數設為 52h；不替你新增缺少的請假日期，也不把夜點與加班總額倒塞進估算公式。其他月份不會套用這些數字。

`private-import/` 與 `verification/` 含薪資轉錄、差額分析或核對案例，只供你自己使用，**不要上傳到公開網站或 GitHub**。網站只需 `site-files/` 的內容。

## 本次修正的範圍

- 移除依固定薪資數字辨識個人、自動注入某月薪資及反推規則的程式。
- 偵測到舊版原樣注入的 39,530／39,280／553 組合時，保留備份，停用這組加班基數、扣薪基數與夜點單價；基數回到固定應領合計。自行設定的其他組合不會被這項遷移改寫。
- 依每日指定類別分算平日、休息日、國定休假日；同一天不重複列入兩種加班。
- 整班請假的晚班不自動計夜點；部分出勤保留待確認，可依公司認定填次數或本期總額。
- 停用加班總時數各拆一半及按總時數比例猜測免稅／應稅金額。
- 覆寫欄位的空白與 0 分開處理。公司薪資條也不把未知欄位默認為 0。
- 檢查薪資條的分項合計、應領、應扣及實領；單有相同實領數字不會宣稱分項已核對。
- 顯示估算來源、逐日計算依據，請假資料未完整載入時明確標示。

目前可確認並修正程式錯誤，也能正確保存這張公司薪資條；**仍不能只憑這些截圖確認日後每個月會自動算到與公司完全相同**。尚缺逐日加班類別、夜點給付規則、公司四捨五入方式及考勤結算起迄。詳見 `private-import/AUGUST_RECONCILIATION.md`。

## 驗證

執行 `node --test tests/*.test.cjs`：79 項通過。涵蓋實際計算函式、薪資條逐項核對、匯入與儲存、零值、月份隔離、請假重疊與撤回、跨年區間、免費天氣，以及五頁中文／印尼文呈現函式。

另檢查 JavaScript 語法與本機資源載入順序。這些是程式與資料測試，未做手機瀏覽器實機操作或畫面驗證。
